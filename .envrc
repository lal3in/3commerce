# direnv environment file
# Run `direnv allow` after editing this file.

source_up

# .NET SDK installed user-locally via dotnet-install.sh (no sudo)
export DOTNET_ROOT="$HOME/.dotnet"
PATH_add "$HOME/.dotnet"
PATH_add "$HOME/.dotnet/tools"
